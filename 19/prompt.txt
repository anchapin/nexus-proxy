You are implementing a fix for GitHub issue #797: P2: Categorize uses substring matching while DSL uses word boundaries — same prompt routes and categorizes differently

Repository: anchapin/nexus-proxy
Branch: fix/issue-797-categorize-word-boundaries (already checked out)
Workdir: /home/alex/AI/worktrees/issue-797-categorize-word-boundaries

Steps:
1. Read the full issue: gh issue view 797
2. Read the issue comments for additional context: gh issue view 797 --comments
3. Analyze what code needs to change
4. Implement the fix/feature with tests
5. Run local checks if available (make test-race, make lint)
6. Commit: git add -A && git commit -m "fix: resolve #797 — use word boundaries in Categorize matching"
7. Push: git push -u origin fix/issue-797-categorize-word-boundaries
8. Open PR with an EXPLICIT body (no `--fill`):
   ```
   gh pr create --base develop \
     --title "fix: resolve #797 — use word boundaries in Categorize matching" \
     --body "$(cat <<'EOF'
   Closes #797

   Categorize now uses word-boundary matching instead of substring matching,
   ensuring consistent routing behavior between Categorize and DSL.
   EOF
   )"
   ```
   The body must contain exactly one `Closes #N` line. Do NOT include other `#NNNN` references.
9. Verify closingReferences count is exactly 1:
   ```
   bash scripts/check_pr_closing_refs.sh <PR_NUMBER> 1
   ```
   If the check fails, run `gh pr edit <PR> --body "Closes #N\n\n<minimal body>"` to strip spurious references, then re-run the check.

Rules:
- Work ONLY in your assigned worktree (/home/alex/AI/worktrees/issue-797-categorize-word-boundaries)
- Do NOT modify files outside the scope of this issue
- Include tests for the fix/feature
- Follow the repo's AGENTS.md and code style conventions
- If the issue is unclear, add a comment asking for clarification: gh issue comment 797 -b "..."
- Report back: PR number, files changed, any blockers encountered

---

Continue if you have next steps, or stop and ask for clarification if you are unsure how to proceed.