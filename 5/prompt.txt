You are implementing a fix for GitHub issue #823: Wave orchestrator: sub-agents complete implementation but skip push/PR creation

Repository: anchapin/nexus-proxy
Branch: fix/issue-823-wave-orch (already checked out)
Workdir: /home/alex/AI/worktrees/issue-823-wave-orch

Steps:
1. Read the full issue: gh issue view 823
2. Read the issue comments for additional context: gh issue view 823 --comments
3. Analyze what code needs to change
4. Implement the fix/feature with tests
5. Run local checks if available (make test-fast, make lint)
6. Commit: git add -A && git commit -m "fix: resolve #823 — fix wave orchestrator sub-agent push/PR creation"
7. Push: git push -u origin fix/issue-823-wave-orch
8. Open PR with an EXPLICIT body:
   ```
   gh pr create --base develop \
     --title "fix: resolve #823 — fix wave orchestrator sub-agent push/PR creation" \
     --body "$(cat <<'EOF'
Closes #823

Fix the wave orchestrator so sub-agents properly push and create PRs after completing implementation.
EOF
   )"
   The body must contain exactly one `Closes #N` line.
9. Verify closingReferences count is exactly 1:
   ```
   bash /home/alex/AI/nexus-proxy/scripts/check_pr_closing_refs.sh <PR_NUMBER> 1
   ```
   If the check fails, run `gh pr edit <PR> --body "Closes #823\n\n<minimal body>"`

Rules:
- Work ONLY in your assigned worktree (/home/alex/AI/worktrees/issue-823-wave-orch)
- Do NOT modify files outside the scope of this issue
- Include tests for the fix if the repo has a test suite
- Follow the repo's AGENTS.md and code style conventions
- Report back: PR number, files changed, any blockers encountered