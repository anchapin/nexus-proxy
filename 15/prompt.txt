You are implementing a fix for GitHub issue #831: [P1] Auth limiter blocked requests emit no Prometheus counter — brute-force activity invisible

Repository: anchapin/nexus-proxy
Branch: fix/issue-831-blocked-counter (already checked out)
Workdir: /home/alex/AI/worktrees/issue-831-blocked-counter

Steps:
1. Read the full issue: gh issue view 831
2. Read the issue comments for additional context: gh issue view 831 --comments
3. Analyze what code needs to change
4. Implement the fix/feature with tests
5. Run local checks if available (make test-fast, make lint)
6. Commit: git add -A && git commit -m "fix: resolve #831 — add Prometheus counter for auth limiter blocked requests"
7. Push: git push -u origin fix/issue-831-blocked-counter
8. Open PR with an EXPLICIT body:
   ```
   gh pr create --base develop \
     --title "fix: resolve #831 — add Prometheus counter for auth limiter blocked requests" \
     --body "$(cat <<'EOF'
Closes #831

Add a Prometheus counter that increments when the auth limiter blocks a request so operators can observe brute-force attack activity.
EOF
   )"
   The body must contain exactly one `Closes #N` line.
9. Verify closingReferences count is exactly 1:
   ```
   bash /home/alex/AI/nexus-proxy/scripts/check_pr_closing_refs.sh <PR_NUMBER> 1
   ```
   If the check fails, run `gh pr edit <PR> --body "Closes #831\n\n<minimal body>"`

Rules:
- Work ONLY in your assigned worktree (/home/alex/AI/worktrees/issue-831-blocked-counter)
- Do NOT modify files outside the scope of this issue
- Include tests for the fix if the repo has a test suite
- Follow the repo's AGENTS.md and code style conventions
- Report back: PR number, files changed, any blockers encountered