You are implementing a fix for GitHub issue #842: [P3] DSL unicodePatterns branch operates on raw prompt while other branches use lower

Repository: anchapin/nexus-proxy
Branch: fix/issue-842-dsl-unicode (already checked out)
Workdir: /home/alex/AI/worktrees/issue-842-dsl-unicode

Steps:
1. Read the full issue: gh issue view 842
2. Read the issue comments for additional context: gh issue view 842 --comments
3. Analyze what code needs to change
4. Implement the fix/feature with tests
5. Run local checks if available (make test-fast, make lint)
6. Commit: git add -A && git commit -m "fix: resolve #842 — normalize prompt case for unicodePatterns DSL branch"
7. Push: git push -u origin fix/issue-842-dsl-unicode
8. Open PR with an EXPLICIT body:
   ```
   gh pr create --base develop \
     --title "fix: resolve #842 — normalize prompt case for unicodePatterns DSL branch" \
     --body "$(cat <<'EOF'
Closes #842

Normalize prompt to lowercase before applying unicodePatterns DSL matching so it behaves consistently with other DSL branches.
EOF
   )"
   The body must contain exactly one `Closes #N` line.
9. Verify closingReferences count is exactly 1:
   ```
   bash /home/alex/AI/nexus-proxy/scripts/check_pr_closing_refs.sh <PR_NUMBER> 1
   ```
   If the check fails, run `gh pr edit <PR> --body "Closes #842\n\n<minimal body>"`

Rules:
- Work ONLY in your assigned worktree (/home/alex/AI/worktrees/issue-842-dsl-unicode)
- Do NOT modify files outside the scope of this issue
- Include tests for the fix if the repo has a test suite
- Follow the repo's AGENTS.md and code style conventions
- Report back: PR number, files changed, any blockers encountered