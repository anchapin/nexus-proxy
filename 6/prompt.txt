You are implementing a fix for GitHub issue #826: [P3] batchCap is hardcoded at 64 — no NEXUS_TRACING_BATCH_SIZE env var

Repository: anchapin/nexus-proxy
Branch: fix/issue-826-tracing-batch (already checked out)
Workdir: /home/alex/AI/worktrees/issue-826-tracing-batch

Steps:
1. Read the full issue: gh issue view 826
2. Read the issue comments for additional context: gh issue view 826 --comments
3. Analyze what code needs to change
4. Implement the fix/feature with tests
5. Run local checks if available (make test-fast, make lint)
6. Commit: git add -A && git commit -m "fix: resolve #826 — add NEXUS_TRACING_BATCH_SIZE env var"
7. Push: git push -u origin fix/issue-826-tracing-batch
8. Open PR with an EXPLICIT body:
   ```
   gh pr create --base develop \
     --title "fix: resolve #826 — add NEXUS_TRACING_BATCH_SIZE env var" \
     --body "$(cat <<'EOF'
Closes #826

Add NEXUS_TRACING_BATCH_SIZE environment variable to configure the OTLP tracing batch size instead of hardcoding 64.
EOF
   )"
   The body must contain exactly one `Closes #N` line.
9. Verify closingReferences count is exactly 1:
   ```
   bash /home/alex/AI/nexus-proxy/scripts/check_pr_closing_refs.sh <PR_NUMBER> 1
   ```
   If the check fails, run `gh pr edit <PR> --body "Closes #826\n\n<minimal body>"`

Rules:
- Work ONLY in your assigned worktree (/home/alex/AI/worktrees/issue-826-tracing-batch)
- Do NOT modify files outside the scope of this issue
- Include tests for the fix if the repo has a test suite
- Follow the repo's AGENTS.md and code style conventions
- Report back: PR number, files changed, any blockers encountered