You are implementing a fix for GitHub issue #828: [P3] Grafana dashboard has no OTLP trace panels — observability story is incomplete

Repository: anchapin/nexus-proxy
Branch: fix/issue-828-grafana-otlp (already checked out)
Workdir: /home/alex/AI/worktrees/issue-828-grafana-otlp

Steps:
1. Read the full issue: gh issue view 828
2. Read the issue comments for additional context: gh issue view 828 --comments
3. Analyze what code needs to change
4. Implement the fix/feature with tests
5. Run local checks if available (make test-fast, make lint)
6. Commit: git add -A && git commit -m "fix: resolve #828 — add OTLP trace panels to Grafana dashboard"
7. Push: git push -u origin fix/issue-828-grafana-otlp
8. Open PR with an EXPLICIT body:
   ```
   gh pr create --base develop \
     --title "fix: resolve #828 — add OTLP trace panels to Grafana dashboard" \
     --body "$(cat <<'EOF'
Closes #828

Add OTLP trace panels to the Grafana dashboard to complete the observability story.
EOF
   )"
   The body must contain exactly one `Closes #N` line.
9. Verify closingReferences count is exactly 1:
   ```
   bash /home/alex/AI/nexus-proxy/scripts/check_pr_closing_refs.sh <PR_NUMBER> 1
   ```
   If the check fails, run `gh pr edit <PR> --body "Closes #828\n\n<minimal body>"`

Rules:
- Work ONLY in your assigned worktree (/home/alex/AI/worktrees/issue-828-grafana-otlp)
- Do NOT modify files outside the scope of this issue
- Include tests for the fix if the repo has a test suite
- Follow the repo's AGENTS.md and code style conventions
- Report back: PR number, files changed, any blockers encountered