You are implementing a fix for GitHub issue #830: [P1] containsWord silently matches all prompts when keyword is empty — routing corruption

Repository: anchapin/nexus-proxy
Branch: fix/issue-830-contains-word (already checked out)
Workdir: /home/alex/AI/worktrees/issue-830-contains-word

Steps:
1. Read the full issue: gh issue view 830
2. Read the issue comments for additional context: gh issue view 830 --comments
3. Analyze what code needs to change
4. Implement the fix/feature with tests
5. Run local checks if available (make test-fast, make lint)
6. Commit: git add -A && git commit -m "fix: resolve #830 — prevent containsWord from matching all prompts when keyword is empty"
7. Push: git push -u origin fix/issue-830-contains-word
8. Open PR with an EXPLICIT body:
   ```
   gh pr create --base develop \
     --title "fix: resolve #830 — prevent containsWord from matching all prompts when keyword is empty" \
     --body "$(cat <<'EOF'
Closes #830

Fix containsWord to return false when the keyword is empty instead of silently matching all prompts, which corrupts routing decisions.
EOF
   )"
   The body must contain exactly one `Closes #N` line.
9. Verify closingReferences count is exactly 1:
   ```
   bash /home/alex/AI/nexus-proxy/scripts/check_pr_closing_refs.sh <PR_NUMBER> 1
   ```
   If the check fails, run `gh pr edit <PR> --body "Closes #830\n\n<minimal body>"`

Rules:
- Work ONLY in your assigned worktree (/home/alex/AI/worktrees/issue-830-contains-word)
- Do NOT modify files outside the scope of this issue
- Include tests for the fix if the repo has a test suite
- Follow the repo's AGENTS.md and code style conventions
- Report back: PR number, files changed, any blockers encountered