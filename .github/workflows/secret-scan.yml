# Secret scanning with gitleaks (issue #124).
#
# Scans every pull request for accidentally committed secrets (API keys,
# bearer tokens, private keys). The proxy handles frontier API keys,
# inbound bearer tokens, and TLS material — all read from env vars at
# runtime, so a real key should never appear in the repo. This job is
# the safety net for when it does.
#
# `.github/gitleaks.toml` extends the default rule set and allowlists
# `.env.example` (placeholders only). The real `.env` is gitignored.
#
# Triggers on pull_request to the integration branches (develop and main)
# so secrets are caught before merge — develop is the default branch and
# the base for every feature/fix PR. Also runs on push to both as a
# backstop.

name: Secret Scan

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

permissions:
  contents: read

jobs:
  gitleaks:
    name: gitleaks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Install gitleaks
        run: |
          curl -sSfL https://github.com/gitleaks/gitleaks/releases/download/v8.30.1/gitleaks_8.30.1_linux_x64.tar.gz | tar -xzf - -C /tmp
          echo "/tmp/gitleaks" >> $GITHUB_PATH

      - name: gitleaks
        run: |
          /tmp/gitleaks git --redact --exit-code=2 --config=.github/gitleaks.toml
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
