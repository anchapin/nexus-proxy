You are implementing a fix for GitHub issue #792: P1: AuthLimiter brute-force protection is a dead letter

Repository: anchapin/nexus-proxy
Branch: fix/issue-792-authlimiter-dead-letter (already checked out)
Workdir: /home/alex/AI/worktrees/issue-792-authlimiter-dead-letter

Steps:
1. Read the full issue: gh issue view 792
2. Read the issue comments for additional context: gh issue view 792 --comments
3. Analyze what code needs to change
4. Implement the fix/feature with tests
5. Run local checks if available (make test-race, make ci)
6. Commit: git add -A && git commit -m "fix: resolve #792 — wire AuthLimiter into auth middleware"
7. Push: git push -u origin fix/issue-792-authlimiter-dead-letter
8. Open PR with an EXPLICIT body (no `--fill`):
   ```
   gh pr create --base develop \
     --title "fix: resolve #792 — wire AuthLimiter into auth middleware" \
     --body "$(cat <<'EOF'
   Closes #792

   Auth middleware now calls IsBlocked() and RecordFailure() on the AuthLimiter,
   making brute-force protection active. Also exposes ClientIPResolver via
   AuthLimiter.Resolver() getter.
   EOF
   )"
   ```
   The body must contain exactly one `Closes #N` line. Do NOT include other `#NNNN` references.
9. Verify closingReferences count is exactly 1:
   ```
   bash scripts/check_pr_closing_refs.sh <PR_NUMBER> 1
   ```
   If the check fails, run `gh pr edit <PR> --body "Closes #N\n\n<minimal body>"` to strip spurious references, then re-run the check.

Rules:
- Work ONLY in your assigned worktree (/home/alex/AI/worktrees/issue-792-authlimiter-dead-letter)
- Do NOT modify files outside the scope of this issue
- Include tests for the fix/feature
- Follow the repo's AGENTS.md and code style conventions
- If the issue is unclear, add a comment asking for clarification: gh issue comment 792 -b "..."
- Report back: PR number, files changed, any blockers encountered