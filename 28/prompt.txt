You are implementing a fix for GitHub issue #841: [P2] parseBoolEnvStr silent fallback on invalid YAML values — toon_unfenced: maybe passes silently

Repository: anchapin/nexus-proxy
Branch: fix/issue-841-parse-bool (already checked out)
Workdir: /home/alex/AI/worktrees/issue-841-parse-bool

Steps:
1. Read the full issue: gh issue view 841
2. Read the issue comments for additional context: gh issue view 841 --comments
3. Analyze what code needs to change
4. Implement the fix/feature with tests
5. Run local checks if available (make test-fast, make lint)
6. Commit: git add -A && git commit -m "fix: resolve #841 — make parseBoolEnvStr fail-fast on invalid YAML values"
7. Push: git push -u origin fix/issue-841-parse-bool
8. Open PR with an EXPLICIT body:
   ```
   gh pr create --base develop \
     --title "fix: resolve #841 — make parseBoolEnvStr fail-fast on invalid YAML values" \
     --body "$(cat <<'EOF'
Closes #841

Make parseBoolEnvStr fail-fast on invalid YAML boolean values instead of silently falling back, so operators are notified of misconfiguration immediately.
EOF
   )"
   The body must contain exactly one `Closes #N` line.
9. Verify closingReferences count is exactly 1:
   ```
   bash /home/alex/AI/nexus-proxy/scripts/check_pr_closing_refs.sh <PR_NUMBER> 1
   ```
   If the check fails, run `gh pr edit <PR> --body "Closes #841\n\n<minimal body>"`

Rules:
- Work ONLY in your assigned worktree (/home/alex/AI/worktrees/issue-841-parse-bool)
- Do NOT modify files outside the scope of this issue
- Include tests for the fix if the repo has a test suite
- Follow the repo's AGENTS.md and code style conventions
- Report back: PR number, files changed, any blockers encountered