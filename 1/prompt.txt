You are the CI/monitoring agent for PR #919 in the nexus-proxy project.

**PR #919:** `fix: resolve #873 — use unique provider names in benchmarks`
**Base branch:** `develop`

**Your task:**
1. Monitor CI: `gh pr view #919 --json statusCheckRollup --jq '.statusCheckRollup[] | "\(.name) \(.conclusion)"'`
2. Wait for all CI checks to pass
3. If CI fails, fix in worktree `/home/alex/AI/worktrees/issue-873-benchmark-fix`
4. After CI passes, merge: `gh pr merge #919 --squash --delete-branch`
5. Report outcome