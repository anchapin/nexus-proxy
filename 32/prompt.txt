You are implementing a fix for GitHub issue #843: [P3] Arbiter cache hit slog calls missing request_id — log correlation broken

Repository: anchapin/nexus-proxy
Branch: fix/issue-843-arbiter-slog (already checked out)
Workdir: /home/alex/AI/worktrees/issue-843-arbiter-slog

Steps:
1. Read the full issue: gh issue view 843
2. Read the issue comments for additional context: gh issue view 843 --comments
3. Analyze what code needs to change
4. Implement the fix/feature with tests
5. Run local checks if available (make test-fast, make lint)
6. Commit: git add -A && git commit -m "fix: resolve #843 — add request_id to arbiter cache hit slog calls"
7. Push: git push -u origin fix/issue-843-arbiter-slog
8. Open PR with an EXPLICIT body:
   ```
   gh pr create --base develop \
     --title "fix: resolve #843 — add request_id to arbiter cache hit slog calls" \
     --body "$(cat <<'EOF'
Closes #843

Add request_id slog attribute to arbiter cache hit log calls so operators can correlate arbiter logs with request traces.
EOF
   )"
   The body must contain exactly one `Closes #N` line.
9. Verify closingReferences count is exactly 1:
   ```
   bash /home/alex/AI/nexus-proxy/scripts/check_pr_closing_refs.sh <PR_NUMBER> 1
   ```
   If the check fails, run `gh pr edit <PR> --body "Closes #843\n\n<minimal body>"`

Rules:
- Work ONLY in your assigned worktree (/home/alex/AI/worktrees/issue-843-arbiter-slog)
- Do NOT modify files outside the scope of this issue
- Include tests for the fix if the repo has a test suite
- Follow the repo's AGENTS.md and code style conventions
- Report back: PR number, files changed, any blockers encountered