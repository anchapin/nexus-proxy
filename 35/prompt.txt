You are implementing a fix for GitHub issue #845: [P3] InjectionScanRoles silent fallback masks operator typos — inconsistent with PromptInjectionMode fail-fast

Repository: anchapin/nexus-proxy
Branch: fix/issue-845-injection-scan-roles (already checked out)
Workdir: /home/alex/AI/worktrees/issue-845-injection-scan-roles

Steps:
1. Read the full issue: gh issue view 845
2. Read the issue comments for additional context: gh issue view 845 --comments
3. Analyze what code needs to change
4. Implement the fix/feature with tests
5. Run local checks if available (make test-fast, make lint)
6. Commit: git add -A && git commit -m "{fix|feat}: resolve #845 — make InjectionScanRoles fail-fast consistent with PromptInjectionMode"
7. Push: git push -u origin fix/issue-845-injection-scan-roles
8. Open PR with an EXPLICIT body (no `--fill` — see PR-body conventions):
   ```
   gh pr create --base develop \
     --title "fix: resolve #845 — make InjectionScanRoles fail-fast consistent with PromptInjectionMode" \
     --body "$(cat <<'EOF'
Closes #845

Make InjectionScanRoles fail-fast when an invalid role is specified, consistent with PromptInjectionMode behavior, instead of silently falling back and masking operator typos.
EOF
   )"
   The body must contain exactly one `Closes #N` line. Do NOT include other `#NNNN` references in the body or title.
9. Verify closingReferences count is exactly 1:
   ```
   bash /home/alex/AI/nexus-proxy/scripts/check_pr_closing_refs.sh <PR_NUMBER> 1
   ```
   If the check fails, run `gh pr edit <PR> --body "Closes #845\n\n<minimal body>"`
   to strip the spurious references, then re-run the check.

Rules:
- Work ONLY in your assigned worktree (/home/alex/AI/worktrees/issue-845-injection-scan-roles)
- Do NOT modify files outside the scope of this issue
- Include tests for the fix/feature if the repo has a test suite
- Follow the repo's AGENTS.md and code style conventions
- If the issue is unclear, add a comment asking for clarification: gh issue comment 845 -b "..."
- Report back: PR number, files changed, any blockers encountered