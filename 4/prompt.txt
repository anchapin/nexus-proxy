You are implementing a fix for GitHub issue #785: TestNewVRAMLimiterContextCancelReleasesBlocked is flaky on loaded CI runners

Repository: anchapin/nexus-proxy
Branch: fix/issue-785-vram-limiter-test-flaky (already checked out)
Workdir: /home/alex/AI/worktrees/issue-785-vram-limiter-test-flaky

Steps:
1. Read the full issue: gh issue view 785
2. Read the issue comments for additional context: gh issue view 785 --comments
3. Analyze what code needs to change
4. Implement the fix/feature with tests
5. Run local checks if available (make test-race, make lint)
6. Commit: git add -A && git commit -m "fix: resolve #785 — replace time.Sleep with sync.WaitGroup in VRAM limiter test"
7. Push: git push -u origin fix/issue-785-vram-limiter-test-flaky
8. Open PR with an EXPLICIT body (no `--fill`):
   ```
   gh pr create --base develop \
     --title "fix: resolve #785 — replace time.Sleep with sync.WaitGroup in VRAM limiter test" \
     --body "$(cat <<'EOF'
   Closes #785

   Replace time.Sleep synchronization with sync.WaitGroup + ready channel so the
   test reliably waits for the waiter goroutine to enter cond.Wait() before
   calling cancel(), eliminating flakiness on loaded CI runners.
   EOF
   )"
   ```
   The body must contain exactly one `Closes #N` line. Do NOT include other `#NNNN` references.
9. Verify closingReferences count is exactly 1:
   ```
   bash scripts/check_pr_closing_refs.sh <PR_NUMBER> 1
   ```
   If the check fails, run `gh pr edit <PR> --body "Closes #N\n\n<minimal body>"` to strip spurious references, then re-run the check.

Rules:
- Work ONLY in your assigned worktree (/home/alex/AI/worktrees/issue-785-vram-limiter-test-flaky)
- Do NOT modify files outside the scope of this issue
- Include tests for the fix/feature
- Follow the repo's AGENTS.md and code style conventions
- If the issue is unclear, add a comment asking for clarification: gh issue comment 785 -b "..."
- Report back: PR number, files changed, any blockers encountered