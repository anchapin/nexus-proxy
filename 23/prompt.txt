You are implementing a fix for GitHub issue #799: P2: ParseThresholdOverrides key-capture drops non-ASCII directory names

Repository: anchapin/nexus-proxy
Branch: fix/issue-799-parse-threshold-overrides (already checked out)
Workdir: /home/alex/AI/worktrees/issue-799-parse-threshold-overrides

Steps:
1. Read the full issue: gh issue view 799
2. Read the issue comments for additional context: gh issue view 799 --comments
3. Analyze what code needs to change
4. Implement the fix/feature with tests
5. Run local checks if available (make test-race ./internal/rag/..., make lint)
6. Commit: git add -A && git commit -m "fix: resolve #799 — normalize UTF-8 in ParseThresholdOverrides env key matching"
7. Push: git push -u origin fix/issue-799-parse-threshold-overrides
8. Open PR with an EXPLICIT body (no `--fill`):
   ```
   gh pr create --base develop \
     --title "fix: resolve #799 — normalize UTF-8 in ParseThresholdOverrides env key matching" \
     --body "$(cat <<'EOF'
   Closes #799

   ParseThresholdOverrides now properly handles non-ASCII directory names by
   normalizing env var key extraction to UTF-8 before matching. Added test
   with NEXUS_RAG_THRESHOLD_代码=0.7.
   EOF
   )"
   ```
   The body must contain exactly one `Closes #N` line. Do NOT include other `#NNNN` references.
9. Verify closingReferences count is exactly 1:
   ```
   bash scripts/check_pr_closing_refs.sh <PR_NUMBER> 1
   ```
   If the check fails, run `gh pr edit <PR> --body "Closes #N\n\n<minimal body>"` to strip spurious references, then re-run the check.

Rules:
- Work ONLY in your assigned worktree (/home/alex/AI/worktrees/issue-799-parse-threshold-overrides)
- Do NOT modify files outside the scope of this issue
- Include tests for the fix/feature
- Follow the repo's AGENTS.md and code style conventions
- If the issue is unclear, add a comment asking for clarification: gh issue comment 799 -b "..."
- Report back: PR number, files changed, any blockers encountered