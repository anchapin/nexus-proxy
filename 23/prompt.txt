You are implementing a fix for GitHub issue #837: [P2] Panel non-streaming function has zero slog visibility for non-cache-hit paths

Repository: anchapin/nexus-proxy
Branch: fix/issue-837-panel-slog (already checked out)
Workdir: /home/alex/AI/worktrees/issue-837-panel-slog

Steps:
1. Read the full issue: gh issue view 837
2. Read the issue comments for additional context: gh issue view 837 --comments
3. Analyze what code needs to change
4. Implement the fix/feature with tests
5. Run local checks if available (make test-fast, make lint)
6. Commit: git add -A && git commit -m "fix: resolve #837 — add slog visibility to Panel non-streaming function"
7. Push: git push -u origin fix/issue-837-panel-slog
8. Open PR with an EXPLICIT body:
   ```
   gh pr create --base develop \
     --title "fix: resolve #837 — add slog visibility to Panel non-streaming function" \
     --body "$(cat <<'EOF'
Closes #837

Add structured logging to the Panel non-streaming function's non-cache-hit paths so operators can observe what the panel is doing for those execution paths.
EOF
   )"
   The body must contain exactly one `Closes #N` line.
9. Verify closingReferences count is exactly 1:
   ```
   bash /home/alex/AI/nexus-proxy/scripts/check_pr_closing_refs.sh <PR_NUMBER> 1
   ```
   If the check fails, run `gh pr edit <PR> --body "Closes #837\n\n<minimal body>"`

Rules:
- Work ONLY in your assigned worktree (/home/alex/AI/worktrees/issue-837-panel-slog)
- Do NOT modify files outside the scope of this issue
- Include tests for the fix if the repo has a test suite
- Follow the repo's AGENTS.md and code style conventions
- Report back: PR number, files changed, any blockers encountered