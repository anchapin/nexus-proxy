You are implementing a fix for GitHub issue #794: P2: CachedEmbedder missing CacheStats/EmbedHitCount — panic risk if stacked with EmbedCache

Repository: anchapin/nexus-proxy
Branch: fix/issue-794-cachedembedder-stats (already checked out)
Workdir: /home/alex/AI/worktrees/issue-794-cachedembedder-stats

Steps:
1. Read the full issue: gh issue view 794
2. Read the issue comments for additional context: gh issue view 794 --comments
3. Analyze what code needs to change
4. Implement the fix/feature with tests
5. Run local checks if available (make test-race ./internal/rag/..., make lint)
6. Commit: git add -A && git commit -m "fix: resolve #794 — add CacheStats and EmbedHitCount to CachedEmbedder"
7. Push: git push -u origin fix/issue-794-cachedembedder-stats
8. Open PR with an EXPLICIT body (no `--fill`):
   ```
   gh pr create --base develop \
     --title "fix: resolve #794 — add CacheStats and EmbedHitCount to CachedEmbedder" \
     --body "$(cat <<'EOF'
   Closes #794

   CachedEmbedder now forwards CacheStats() and EmbedHitCount() to the inner
   *EmbedCache when present, returning zeros otherwise. This prevents panics
   when CachedEmbedder is stacked with EmbedCache.
   EOF
   )"
   ```
   The body must contain exactly one `Closes #N` line. Do NOT include other `#NNNN` references.
9. Verify closingReferences count is exactly 1:
   ```
   bash scripts/check_pr_closing_refs.sh <PR_NUMBER> 1
   ```
   If the check fails, run `gh pr edit <PR> --body "Closes #N\n\n<minimal body>"` to strip spurious references, then re-run the check.

Rules:
- Work ONLY in your assigned worktree (/home/alex/AI/worktrees/issue-794-cachedembedder-stats)
- Do NOT modify files outside the scope of this issue
- Include tests for the fix/feature
- Follow the repo's AGENTS.md and code style conventions
- If the issue is unclear, add a comment asking for clarification: gh issue comment 794 -b "..."
- Report back: PR number, files changed, any blockers encountered