You are implementing a fix for GitHub issue #803: P3: OTLP retry/backoff parameters are hardcoded — operators cannot tune for high-latency collectors

Repository: anchapin/nexus-proxy
Branch: fix/issue-803-otlp-retry-backoff (already checked out)
Workdir: /home/alex/AI/worktrees/issue-803-otlp-retry-backoff

Steps:
1. Read the full issue: gh issue view 803
2. Read the issue comments for additional context: gh issue view 803 --comments
3. Analyze what code needs to change
4. Implement the fix/feature with tests
5. Run local checks if available (make test-race, make lint)
6. Commit: git add -A && git commit -m "feat: resolve #803 — make OTLP retry/backoff parameters configurable via env vars"
7. Push: git push -u origin fix/issue-803-otlp-retry-backoff
8. Open PR with an EXPLICIT body (no `--fill`):
   ```
   gh pr create --base develop \
     --title "feat: resolve #803 — make OTLP retry/backoff parameters configurable" \
     --body "$(cat <<'EOF'
   Closes #803

   Add NEXUS_TRACING_MAX_RETRIES, NEXUS_TRACING_RETRY_BASE_DELAY, and
   NEXUS_TRACING_RETRY_MAX_DELAY env vars to allow operators to tune OTLP
   retry behavior for high-latency collectors.
   EOF
   )"
   ```
   The body must contain exactly one `Closes #N` line. Do NOT include other `#NNNN` references.
9. Verify closingReferences count is exactly 1:
   ```
   bash scripts/check_pr_closing_refs.sh <PR_NUMBER> 1
   ```
   If the check fails, run `gh pr edit <PR> --body "Closes #N\n\n<minimal body>"` to strip spurious references, then re-run the check.

Rules:
- Work ONLY in your assigned worktree (/home/alex/AI/worktrees/issue-803-otlp-retry-backoff)
- Do NOT modify files outside the scope of this issue
- Include tests for the fix/feature
- Follow the repo's AGENTS.md and code style conventions
- Update docs/observability-surface.md with the new env vars
- If the issue is unclear, add a comment asking for clarification: gh issue comment 803 -b "..."
- Report back: PR number, files changed, any blockers encountered

---

Continue if you have next steps, or stop and ask for clarification if you are unsure how to proceed.