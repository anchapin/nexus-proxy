You are implementing a fix for GitHub issue #829: [P1] HNSW index permanently invalidated after upsert — O(n) regression

Repository: anchapin/nexus-proxy
Branch: fix/issue-829-hnsw-upsert (already checked out)
Workdir: /home/alex/AI/worktrees/issue-829-hnsw-upsert

Steps:
1. Read the full issue: gh issue view 829
2. Read the issue comments for additional context: gh issue view 829 --comments
3. Analyze what code needs to change
4. Implement the fix/feature with tests
5. Run local checks if available (make test-fast, make lint)
6. Commit: git add -A && git commit -m "fix: resolve #829 — fix HNSW index invalidation after upsert"
7. Push: git push -u origin fix/issue-829-hnsw-upsert
8. Open PR with an EXPLICIT body:
   ```
   gh pr create --base develop \
     --title "fix: resolve #829 — fix HNSW index invalidation after upsert" \
     --body "$(cat <<'EOF'
Closes #829

Fix HNSW index to not be permanently invalidated after an upsert operation, which was causing an O(n) performance regression.
EOF
   )"
   The body must contain exactly one `Closes #N` line.
9. Verify closingReferences count is exactly 1:
   ```
   bash /home/alex/AI/nexus-proxy/scripts/check_pr_closing_refs.sh <PR_NUMBER> 1
   ```
   If the check fails, run `gh pr edit <PR> --body "Closes #829\n\n<minimal body>"`

Rules:
- Work ONLY in your assigned worktree (/home/alex/AI/worktrees/issue-829-hnsw-upsert)
- Do NOT modify files outside the scope of this issue
- Include tests for the fix if the repo has a test suite
- Follow the repo's AGENTS.md and code style conventions
- Report back: PR number, files changed, any blockers encountered