You are implementing a fix for GitHub issue #796: P2: Panel silently swallows malformed arbiter responses — empty choices treated as cache miss

Repository: anchapin/nexus-proxy
Branch: fix/issue-796-panel-malformed-arbiter (already checked out)
Workdir: /home/alex/AI/worktrees/issue-796-panel-malformed-arbiter

Steps:
1. Read the full issue: gh issue view 796
2. Read the issue comments for additional context: gh issue view 796 --comments
3. Analyze what code needs to change
4. Implement the fix/feature with tests
5. Run local checks if available (make test-race, make lint)
6. Commit: git add -A && git commit -m "fix: resolve #796 — return error when arbiter returns empty synthesis"
7. Push: git push -u origin fix/issue-796-panel-malformed-arbiter
8. Open PR with an EXPLICIT body (no `--fill`):
   ```
   gh pr create --base develop \
     --title "fix: resolve #796 — return error when arbiter returns empty synthesis" \
     --body "$(cat <<'EOF'
   Closes #796

   Panel now returns an error when arbiter returns valid HTTP 200 but empty
   synthesis (choices missing or empty), preventing silent fallthrough to
   writeCachedArbiterJSON.
   EOF
   )"
   ```
   The body must contain exactly one `Closes #N` line. Do NOT include other `#NNNN` references.
9. Verify closingReferences count is exactly 1:
   ```
   bash scripts/check_pr_closing_refs.sh <PR_NUMBER> 1
   ```
   If the check fails, run `gh pr edit <PR> --body "Closes #N\n\n<minimal body>"` to strip spurious references, then re-run the check.

Rules:
- Work ONLY in your assigned worktree (/home/alex/AI/worktrees/issue-796-panel-malformed-arbiter)
- Do NOT modify files outside the scope of this issue
- Include tests for the fix/feature
- Follow the repo's AGENTS.md and code style conventions
- If the issue is unclear, add a comment asking for clarification: gh issue comment 796 -b "..."
- Report back: PR number, files changed, any blockers encountered